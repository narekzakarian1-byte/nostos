node_modules/
dist/
.vite/
coverage/
*.local
.env
.env.*
.DS_Store
*.log
.devsmoke/

# Собранный вырезатель фона (tools/matte.swift). Артефакт сборки.
tools/matte

# Кадры из tools/play.mjs. Артефакт прогона, не исходник.
shots/
shots-*/

# Исходники генерации: сырые 1024×1024 по 1-2.5 МБ на файл, по дюжине на
# остров. В игру идут вырезанные и ужатые копии из public/art (~160 КБ),
# они и версионируются. Сами исходники остаются на диске: пересобрать
# вырез можно без повторной генерации.
islands/uploads/**/*.png
