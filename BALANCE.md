# NOSTOS — балансная математика

Все числа, названные здесь, живут в `balance.json`. В коде не должно быть ни одной магической константы.

Часть коэффициентов выведена из скриншотов референса — они помечены как **[из референса]**.

---

## 1. Конверсия сырых статов в проценты

В референсе крит показан как `139 (16.8%)`, уклонение как `18 (8.49%)`, урон крита как `82 (213%)`. Значит там нелинейная конверсия с насыщением. Восстановленные формулы:

```
crit_chance  = CRIT_CAP  * raw / (raw + K_CRIT)
dodge_chance = DODGE_CAP * raw / (raw + K_DODGE)
crit_mult    = CRIT_MULT_BASE + raw_critdmg / K_CRITDMG
```

| Константа | Значение | Проверка |
|---|---|---|
| `CRIT_CAP` | 0.75 | — |
| `K_CRIT` | 480 | raw 139 → 16.8% **[из референса]** |
| `DODGE_CAP` | 0.60 | — |
| `K_DODGE` | 110 | raw 18 → 8.5% **[из референса]** |
| `CRIT_MULT_BASE` | 1.50 | — |
| `K_CRITDMG` | 130 | raw 82 → 213% **[из референса]** |

Смысл насыщения: первые вложения в крит дают много, дальше всё меньше. Это не даёт стату сломать баланс на поздних островах.

---

## 2. Формула размена

### 2.1 Урон одного оружия

```
ratio_i = ATK_i / max(DEF_i, 1)
mult_i  = ratio_i^E / (1 + ratio_i^E)
dmg_i   = ATK_i * clamp(mult_i, DMG_FLOOR, DMG_CEIL)
```

`E = 1.5` — крутизна треугольника типов.
`DMG_FLOOR = 0.05`, `DMG_CEIL = 0.95`.

Что даёт `E = 1.5`:

| ratio (атака / защита) | множитель | смысл |
|---|---|---|
| 0.33 | 0.16 | красная иконка, бьёшь почти впустую |
| 0.5 | 0.26 | красная |
| 1.0 | 0.50 | серая, нейтрально |
| 1.5 | 0.65 | зелёная |
| 2.0 | 0.74 | зелёная |
| 3.0 | 0.84 | зелёная, рвёшь |

Разница между «неправильным» и «правильным» типом — примерно **5 раз**. Это ровно столько, сколько нужно: иконки имеют вес, но неправильный тип не превращается в ноль.

`E` — главный рычаг «насколько важен выбор типа». Поднять до 2.0 — типы станут почти обязательными. Опустить до 1.0 — станут декоративными.

### 2.2 Суммарный DPS

```
crit_factor = 1 + crit_chance * (crit_mult - 1)
DPS = Σ_i (dmg_i * attack_speed_i) * crit_factor
```

`attack_speed` базовая: 1.0 удара в секунду на оружие. Стат «скорость» влияет на передвижение, **не** на скорость атаки — иначе он становится множителем ко всему сразу и ломает баланс.

### 2.3 Входящий урон

Треугольник симметричен: враг бьёт тем же типом, к которому уязвим, а против него стоит твоя защита этого типа, помноженная на броню.

```
DEF_i        = stat_def[i] * armor_mult_i
ratio_in     = enemy_DPS / max(DEF_i, 1)
mult_in      = ratio_in^E / (1 + ratio_in^E)
incoming_DPS = enemy_DPS * clamp(mult_in, DMG_FLOOR, DMG_CEIL) * (1 - dodge_chance)
```

Формула и все константы — те же, что у исходящего урона в §2.1. Две разные кривые здесь означали бы, что игрок читает иконки, описывающие только половину боя.

Первая версия считала `incoming_DPS = enemy_DPS * (1 - dodge_chance)`, без защиты вообще. Тогда три защиты из одиннадцати характеристик росли с убитых и не влияли ни на что, а выжить можно было только здоровьем — почему на этом не сходился гейт, см. §7.5.

---

## 3. Пороги иконок над врагом

```
ratio >= ICON_GREEN_AT  (1.5)  → зелёная
ratio <= ICON_RED_AT    (0.7)  → красная
иначе                          → серая
```

Иконка считается по **фактическому** оружию в слоте против **фактической** защиты врага. Никаких приближений — игрок должен доверять иконкам абсолютно.

---

## 4. Реген

```
Вне боя (нет полученного и нанесённого урона T_OUT секунд):
    HP += maxHP * REGEN_RATE * dt
```

| Кто | `T_OUT` | `REGEN_RATE` | Полное восстановление |
|---|---|---|---|
| Игрок | 3.0 с | 0.040 /с | 25 с |
| Обычный враг | 4.0 с | 0.060 /с | 17 с |
| Мини-босс | 5.0 с | 0.045 /с | 22 с |

Враг регенерирует быстрее игрока намеренно: отступление от почти убитого противника должно быть болезненным. Это и есть весь скилл-слой игры, поэтому цифры здесь самые чувствительные в проекте.

**Островной босс — особый случай:** регенерирует **в бою**, всегда.

```
boss_regen_flat = boss_maxHP * BOSS_COMBAT_REGEN   (0.012 /с)
```

---

## 5. Гейт: главная математика проекта

### 5.1 Условие победы

```
net_DPS = player_DPS - boss_maxHP * BOSS_COMBAT_REGEN

net_DPS <= 0  →  победить невозможно, шкала стоит
net_DPS >  0  →  время убийства T_kill = boss_maxHP / net_DPS
```

Одна константа `BOSS_COMBAT_REGEN` крутит сложность всех гейтов сразу.

### 5.2 Время до смерти игрока

```
T_death = player_HP_eff / (boss_DPS * (1 - dodge))
```

**Требование: `T_death >= 40` секунд** при силе «только что зачистил остров». Быстрее — игрок не успевает понять, что произошло, и читает гейт как несправедливость.

### 5.3 Процент, снятый за попытку

```
progress% = min(100, net_DPS * T_death / boss_maxHP * 100)
```

**Целевые значения:**

| Момент | progress% |
|---|---|
| Первая попытка сразу после зачистки острова | **40–60%** |
| После прокачки оружия на 3–5 уровней | 70–85% |
| После смены на правильный тип урона | 100% |

Проверка на безнадёгу: если при первой попытке `progress% < 30` — босс перетюнен, снижай `BOSS_COMBAT_REGEN` или HP босса.
Проверка на бессмысленность: если `> 70` — гейт не работает и не приносит денег.

### 5.4 Посчитанные требования по островам

Расчёт при `T_death = 45 с` и целевом прогрессе 50% за попытку:

| Остров | HP босса | DPS босса | Реген босса /с | Нужен DPS игрока | Нужно HP игрока |
|---|---|---|---|---|---|
| 1 | 6 000 | 32 | 72 | 139 | 1 440 |
| 2 | 12 000 | 64 | 144 | 277 | 2 880 |
| 3 | 24 000 | 128 | 288 | 555 | 5 760 |
| 4 | 48 000 | 256 | 576 | 1 109 | 11 520 |
| 5 | 96 000 | 512 | 1 152 | 2 219 | 23 040 |
| 7 | 384 000 | 2 048 | 4 608 | 8 875 | 92 160 |
| 10 | 3 072 000 | 16 384 | 36 864 | 70 997 | 737 280 |
| 13 | 24 576 000 | 131 072 | 294 912 | 567 979 | 5 898 240 |

Порядок финальных чисел (сотни тысяч DPS, миллионы HP) совпадает с референсом, где на скриншотах DPS 201K и HP 381K. Значит масштаб выбран верно.

**Калибровка острова 3 (Полифем):** нужен DPS 555. Достигается при накопленном стате атаки ≈ 100 и зелёном оружии 10 уровня — это как раз результат зачистки острова плюс два-три круга по мини-боссам. Сходится.

### 5.5 Разрыв силы

Отношение «сила, нужная для победы над боссом» к «силе после зачистки острова»:

- острова 1–4: **1.5–2.0×**
- острова 5–9: **2.0–2.5×**
- острова 10–13: **2.5–3.0×**

---

## 6. Кривая островов

```
island_power(n) = POWER_BASE ^ (n - 1)        POWER_BASE = 2.0

enemy_maxHP = BASE_HP  * island_power(n) * type_mult
enemy_DPS   = BASE_DPS * island_power(n) * type_mult
```

`BASE_HP = 100`, `BASE_DPS = 8`.

Множители по типам врага:

| Тип | HP | DPS | Респаун |
|---|---|---|---|
| Обычный | 1.0 | 1.0 | 60–120 с |
| Элитный | 4.0 | 1.8 | 8–15 мин |
| Мини-босс | 12.0 | 2.5 | 15–30 мин |
| Островной босс | 60.0 | 4.0 | 4–12 ч |

За 13 островов разброс силы составит `2^12 ≈ 4100×`. С учётом множителей типов верхняя граница выходит на сотни тысяч — тот же порядок, что в референсе (HP 381K, DPS 201K).

---

## 7. Оружие

### 7.1 Атака — оружие множит стат, а не прибавляется к нему

```
weapon_mult = rarity_mult * (1 + LEVEL_STEP * (level - 1))
ATK_type    = stat_atk[type] * weapon_mult
```

`LEVEL_STEP = 0.055`

**Это принципиально.** Первая версия расчёта была аддитивной (`ATK = стат + оружие`), и симулятор показал, что уже к третьему острову накопленные статы настолько превышают базу оружия, что прокачка с 10 до 25 уровня меняет урон на 11%. Оружие становится декорацией, а система копий — бессмысленной.

При множительной модели (стат 100, зелёное оружие, остров 3):

| Уровень оружия | Множитель | DPS |
|---|---|---|
| 1 | 1.60 | 317 |
| 10 | 2.39 | 552 |
| 20 | 3.27 | 817 |
| 30 | 4.15 | 1084 |

Прокачка с 1 до 30 уровня даёт рост урона в **3.4 раза**. Копии снова значат всё.

| Редкость | Множитель |
|---|---|
| Обычное | 1.00 |
| Зелёное | 1.60 |
| Синее | 2.60 |
| Фиолетовое | 4.20 |
| Золотое | 6.80 |

Шаг между редкостями ≈ 1.62. Уровень 50 даёт ×3.70.

### 7.2 Наследование уровня — обязательное правило

```
При получении оружия более высокой редкости того же типа:
    new_weapon.level = max(new_weapon.level, best_owned_of_type.level)
```

Проблема референса, посчитанная в числах:

| Что было | Множитель | Что падает | Без наследования | С наследованием |
|---|---|---|---|---|
| Синее ур. 15 | 4.6× | Фиолетовое | 4.2× — **хуже** | 7.4× |
| Синее ур. 30 | 6.7× | Фиолетовое | 4.2× — **хуже** | 10.9× |
| Синее ур. 50 | 9.6× | Фиолетовое | 4.2× — **хуже** | 15.5× |

Без наследования лучший дроп в игре делает игрока **слабее** — во всех трёх случаях. Это ровно то, что видно на скриншотах референса, где фиолетовое оружие 1 уровня лежит рядом с синим 50-го.

С наследованием редкий дроп даёт мгновенный рост в 1.6 раза. Гринд копий при этом не сокращается ни на одну копию.

### 7.3 Копии

```
copies_for_next_level(L) = ceil(COPY_A * L^COPY_B + COPY_C)
```

**Продакшн:** `COPY_A = 0.8`, `COPY_B = 1.4`, `COPY_C = 1`

| Уровень | Копий на след. |
|---|---|
| 1 → 2 | 3 |
| 5 → 6 | 9 |
| 10 → 11 | 21 |
| 15 → 16 | 36 |
| 30 → 31 | 96 |
| 50 → 51 | 205 |

Порядок совпадает с референсом, где на скриншоте видно `2/400`, но кривая заметно мягче.

**Прототип:** `COPY_A = 0.25`, `COPY_B = 1.15` — примерно вчетверо быстрее, чтобы весь цикл можно было проверить за час, а не за неделю.

### 7.4 Дроп копий

```
Мини-босс:    3–8 копий своего типа
Элитный:      0–2 копии (шанс 35%)
Обычный:      0
```

На острове **5 мини-боссов**. Один круг зачистки при респауне 15–30 минут даёт примерно **25 копий за 15 минут**, то есть около **100 копий в час**.

Отсюда время на прокачку одного оружия с 1 до 15 уровня ≈ **270 копий ≈ 2.5–3 часа**. Это и есть темп острова.

---

### 7.5 Броня — второй множитель

Броня относится к защите ровно так же, как оружие к атаке:

```
armor_mult = rarity_mult * (1 + ARMOR_LEVEL_STEP * (level - 1))
DEF_type   = stat_def[type] * armor_mult
```

Ступени редкости и наследование уровня — те же, что у оружия (§7.2). Валюта своя: осколки брони по трём типам, отдельно от копий оружия.

**Почему без неё гейт не сводится.** Атака игрока растёт двумя системами сразу: прирост с убитых, помноженный на оружие. Здоровье и защита росли одной — только приростом с убитых. Враг растёт по одной экспоненте `POWER_BASE^(n-1)` и по HP, и по DPS, а §5.4 требует, чтобы под неё подстроились обе стороны игрока одновременно.

Перебор параметров показал, что одним `GAIN_RATE` это неразрешимо. Он множит оба трека одинаково, но над треком атаки стоит ещё оружейный множитель (к финалу до ×14), а над треком выживания — ничего. Замеры на симуляторе, остальные числа стоковые:

| `GAIN_RATE` | T_death остров 1 | T_death остров 13 | Прогресс первой попытки | В коридоре 40–60% |
|---|---|---|---|---|
| ×1 | 11 с | 1 с | скачет 0–100% | 5 из 13 |
| ×15 | 34 с | 3 с | 100% везде | 0 из 13 |
| ×65 | 101 с | 6 с | 100% везде | 0 из 13 |
| ×145 | 195 с | 10 с | 100% везде | 0 из 13 |

Даже при ×145 тринадцатый остров даёт 10 секунд до смерти против порога в 40, а боссы к этому моменту падают с первой попытки везде. Трек выживания упирается в потолок раньше, чем догоняет экспоненту врага, потому что у него нет множителя. Броня его даёт.

---

## 8. Рост игрока

Каждый убитый враг даёт перманентный прирост статов — как в референсе, мгновенно и прямо в бою, без экрана результатов.

```
stat_gain = enemy_tier_value * GAIN_RATE * diminish(current_stat)
diminish(x) = 1 / (1 + x / DIMINISH_K)
```

`GAIN_RATE = 1.0`, `DIMINISH_K = 5000`

Затухание нужно, чтобы фарм самых слабых врагов на 10-м острове не был выгоднее боя с сильными. Без него игроки находят эксплойт за сутки.

**Что именно падает с врага** — зависит от его типа, как в референсе:

| Враг | Даёт |
|---|---|
| Быстрый | скорость, уклонение |
| Бронированный | защиты |
| Тяжёлый | здоровье, реген |
| Атакующий | урон, крит |

---

## 9. Реперные точки

| Момент | Что должно произойти |
|---|---|
| 0:00–0:10 | Первое убийство |
| 0:30 | Первый апгрейд, первое видимое изменение |
| 0:40 | Игрок понял смысл иконок над врагом (без туториала) |
| 3:00 | Первый элитный, первая красная иконка, первое поражение |
| 10:00 | Первый мини-босс, первые копии, первый уровень оружия |
| 40:00 | Первый островной босс. Первая попытка — 40–60% |
| 60:00 | Босс убит, остров пройден, первый трофей на корабле |
| 60:00+ | Первое предложение покупки, не раньше |

Правило: **если игрок 5 минут не получил ничего нового — это баг дизайна, а не хардкор.**

---

## 10. Симулятор баланса — обязательная часть прототипа

Отдельный headless-скрипт, который прогоняет прогрессию без графики и печатает отчёт. Без него баланс придётся переделывать трижды вручную.

Что он должен выводить:

```
ОСТРОВ 3 — Полифем
  Сила игрока после зачистки:      DPS 1840   HP 12400
  Босс:                            HP 72000   DPS 310
  Реген босса:                     864 /с
  Чистый DPS:                      976 /с
  Время до смерти игрока:          48.1 с   [OK, >= 40]
  Прогресс за попытку:             65.2%    [ВЫСОКО, цель 40-60]
  Копий до победы:                 ~180
  Время подготовки:                ~1.8 ч   [OK]
```

Автоматические проверки, которые скрипт валит с ошибкой:

1. `T_death >= 40` секунд на каждом острове
2. `progress%` первой попытки в диапазоне 40–60 на каждом острове
3. время подготовки к боссу растёт монотонно и не превышает 12 часов игрового времени
4. ни на одном острове нет `net_DPS <= 0` при полностью прокачанном под остров снаряжении
5. фарм слабых врагов не выгоднее фарма сильных (проверка затухания)

---

## 11. Порядок настройки

Крутить строго в этом порядке, иначе будешь ловить свой хвост:

1. **`E`** — важность типов урона. Настраивается на глаз за 10 минут игры.
2. **`REGEN_RATE` и `T_OUT`** — ощущение «дожать или отойти». Самая чувствительная пара цифр в проекте.
3. **`POWER_BASE`** — общий темп игры.
4. **`BOSS_COMBAT_REGEN`** — жёсткость всех гейтов одной цифрой.
5. **`COPY_A`, `COPY_B`** — длина гринда. Настраивается последней, когда всё остальное ощущается правильно.

Никогда не крути две группы одновременно.
